# Thermal Printer Architecture & Flow Diagrams

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                    POS CANDY KUSH SYSTEM                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────────┐          ┌──────────────────┐            │
│  │   Web Admin      │          │  Main Activity   │            │
│  │   Interface      │          │  (Primary Screen)│            │
│  └────────┬─────────┘          └────────┬─────────┘            │
│           │                             │                      │
│           │ Create Print Job            │ Cart Updates         │
│           └─────────────────────────────┼──────────┐           │
│                                         │          │           │
│                                    ┌────▼──────────▼─────┐    │
│                                    │  CartApiService     │    │
│                                    │  (Polls Cart API)   │    │
│                                    └────────┬────────────┘    │
│                                             │                 │
│                                ┌────────────▼──────────────┐  │
│                                │ CustomerPresentation      │  │
│                                │ (Secondary Display)       │  │
│                                │ ┌──────────────────────┐  │  │
│                                │ │ - Current Cart Data  │  │  │
│                                │ │ - Print Job Polling  │  │  │
│                                │ │ - Bluetooth Control  │  │  │
│                                │ └──────────────────────┘  │  │
│                                └────────────┬──────────────┘  │
│                                             │                 │
│                                    ┌────────▼──────────┐      │
│                                    │ PrintApiService   │      │
│                                    │ (Polls Print API) │      │
│                                    └────────┬──────────┘      │
│                                             │                 │
│                                    ┌────────▼────────────────┐│
│                                    │BluetoothThermalPrinter  ││
│                                    │ - Connect to Printer    ││
│                                    │ - Format Receipt        ││
│                                    │ - Send Data             ││
│                                    └────────┬────────────────┘│
│                                             │                 │
└─────────────────────────────────┼──────────┼─────────────────┘
                                  │          │
                    ┌─────────────┼──────────┤
                    │             │          │
         ┌──────────▼──┐  ┌──────▼──┐  ┌──▼───────┐
         │  Print API  │  │Bluetooth│  │ Thermal  │
         │  Endpoint   │  │ Device  │  │ Printer  │
         └─────────────┘  └─────────┘  └──────────┘
```

## Complete Print Job Flow

```
STEP 1: ORDER CREATION
┌─────────────────────────────────────────────────────────┐
│ Web Interface / Admin Panel                             │
│ - Creates order with items                              │
│ - Decides to print receipt                              │
│ - Sends print command to API                            │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
STEP 2: PRINT JOB CREATION
┌─────────────────────────────────────────────────────────┐
│ API: POST /api/print                                    │
│ Request: { "data": "THERMAL_PRINTER:print_data" }       │
│ Response: { "success": true, "message": "..." }         │
│ Database: Print job created and stored                  │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
STEP 3: CUSTOMER PRESENTATION ACTIVE
┌─────────────────────────────────────────────────────────┐
│ CustomerPresentation (2nd Screen)                       │
│ - Displays current cart                                 │
│ - onCreate() starts print job polling                   │
│ - Handler posts checkForPrintJob every 2000ms           │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
STEP 4: POLLING LOOP (Every 2 seconds)
┌─────────────────────────────────────────────────────────┐
│ printCheckRunnable (Background Thread)                  │
│ - Calls PrintApiService.getPrintJob()                   │
│ - Sends: GET /api/print                                 │
│ - Response 1: null/empty → Continue polling              │
│ - Response 2: data found → Go to STEP 5                 │
└────────────────────┬────────────────────────────────────┘
                     │
         ┌───────────┴──────────────┐
         │ No print job            │ Print job found
         │                          │
         ▼                          ▼
    Continue Loop            STEP 5: HANDLE PRINT JOB
                     ┌─────────────────────────────────────────┐
                     │ handlePrintJob(printData)                │
                     │ - Extract printer device name            │
                     │ - Check Bluetooth connection             │
                     │ - Connect if needed                      │
                     └────────────────┬────────────────────────┘
                                      │
                                      ▼
                     ┌─────────────────────────────────────────┐
                     │ bluetoothPrinter.printReceipt(...)       │
                     │ - buildReceiptData(cartItems, total)     │
                     │ - Format with "CANDY KUSH" title         │
                     │ - Add items, totals, timestamp           │
                     │ - Generate ESC/POS commands              │
                     └────────────────┬────────────────────────┘
                                      │
                                      ▼
STEP 6: SEND TO PRINTER
                     ┌─────────────────────────────────────────┐
                     │ sendData(receiptData)                    │
                     │ - Write to Bluetooth socket              │
                     │ - Flush output stream                    │
                     │ - Log success                            │
                     └────────────────┬────────────────────────┘
                                      │
                                      ▼
STEP 7: PRINTER OUTPUT
                     ┌─────────────────────────────────────────┐
                     │ Thermal Printer                          │
                     │ - Receives ESC/POS commands              │
                     │ - Formats text                           │
                     │ - Prints receipt                         │
                     │ - Executes paper cut command             │
                     └─────────────────────────────────────────┘
                                      │
                                      ▼
STEP 8: JOB CLEANUP
                     ┌─────────────────────────────────────────┐
                     │ API Automatically:                       │
                     │ - Deletes print job from database        │
                     │ - Prevents duplicate prints              │
                     │ - Returns success response               │
                     └────────────────┬────────────────────────┘
                                      │
                                      ▼
STEP 9: LOOP CONTINUES
                     ┌─────────────────────────────────────────┐
                     │ Polling resumes (back to STEP 4)         │
                     │ - Checks for next print job              │
                     │ - Every 2 seconds                        │
                     └─────────────────────────────────────────┘
```

## Component Interaction Diagram

```
                    CartViewModel
                         │
                ┌────────┴────────┐
                │                 │
           StartPolling      cartState (observed)
                │                 │
                ▼                 ▼
          CartApiService    MainActivity/
          (polls cart)       CustomerPresentation
                                  │
                    ┌─────────────┼────────────┐
                    │             │            │
            updateCart()   currentCart     printCheckRunnable
                │           stored         (polls every 2s)
                │                                 │
                ▼                                 ▼
         Display Updated              PrintApiService
         Cart Data                    (checks for jobs)
                                             │
                                    ┌────────┴────────┐
                                    │                 │
                                 No Job           Job Found
                                    │                 │
                              Continue          handlePrintJob()
                              Polling                 │
                                                      ▼
                                           Extract Printer Name
                                                      │
                                         ┌────────────┴────────┐
                                         │                     │
                                   Connected          Not Connected
                                         │                     │
                                         │            bluetoothPrinter
                                         │            .connect(name)
                                         │                     │
                                         └────────────┬────────┘
                                                      │
                                                      ▼
                                              printReceipt()
                                                      │
                                            buildReceiptData()
                                                      │
                                                      ▼
                                              sendData(bytes)
                                                      │
                                                      ▼
                                                 Bluetooth
                                                  Socket
                                                      │
                                                      ▼
                                              🖨️ Thermal Printer
```

## Data Flow for Receipt

```
INPUT: Cart Data
┌──────────────────┐
│ items:           │
│  - id            │
│  - name          │
│  - quantity      │
│  - price         │
│  - total         │
│ total: ฿24.00    │
│ lastUpdated: ... │
└────────┬─────────┘
         │
         ▼
buildReceiptData() Function
         │
    ┌────┴────────────────────┬─────────────────┐
    │                         │                 │
    ▼                         ▼                 ▼
Header              Item Loop           Footer
────────────────    ──────────────      ──────
CANDY KUSH          For each item:      Thank you!
═════════════       - Format name       Timestamp
                    - Format qty        Paper cut
                    - Format total      ═════════
                    - Add to receipt
                         │
                         ▼
                    Calculate totals
                    ─────────────────
                    TOTAL: ฿24.00
                    
OUTPUT: ByteArray (UTF-8 encoded)
┌────────────────────────────────────────┐
│ ════════════════════════════════        │
│              CANDY KUSH                 │
│ ════════════════════════════════        │
│                                        │
│ ITEM              QTY       TOTAL       │
│ ─────────────────────────────────      │
│ L.C.G           0.20      ฿24.00       │
│ ─────────────────────────────────      │
│                    TOTAL: ฿24.00       │
│                                        │
│                Thank you!               │
│            2025-11-13 13:36:54          │
│ ════════════════════════════════        │
│                                        │
└────────────────────────────────────────┘
         │
         ▼
    sendData(bytes)
         │
         ▼
  Bluetooth Socket Output Stream
         │
         ▼
   Thermal Printer
```

## Thread Flow

```
Main Thread (UI)
├─ onCreate()
│  └─ handler.post(printCheckRunnable) ──►
│
├─ updateCart() called from lifecycle
│  └─ Stores currentCart
│
└─ onDetachedFromWindow()
   └─ handler.removeCallbacks(printCheckRunnable) ──►


Background Thread (Print Polling)
┌─────────────────────────────────────────────────────────┐
│ printCheckRunnable (Every 2 seconds)                    │
└─────────────────────────────────────────────────────────┘
   │
   └─► checkForPrintJob() runs on new Thread {
        │
        ├─ PrintApiService.getPrintJob()
        │  └─ OkHttp makes synchronous GET request
        │     └─ Returns PrintJobResponse
        │
        ├─ if (job found)
        │  └─ handlePrintJob(data)
        │     └─ Thread continues
        │
        └─ catch exceptions and log
       }
       
   └─► handler.postDelayed(this, 2000)
       └─ Reschedules itself


Bluetooth Thread (Implied)
┌─────────────────────────────────────────────────────────┐
│ bluetoothSocket connects in parallel                    │
│ - Non-blocking on modern Android                        │
│ - Can happen while main thread continues                │
└─────────────────────────────────────────────────────────┘
```

## Error Handling Flow

```
checkForPrintJob()
       │
       ▼
   try {
       │
       ├─ getPrintJob() → null
       │  └─ Log error, continue
       │
       ├─ ParseException
       │  └─ Log parse error, continue
       │
       └─ NetworkException
          └─ Log network error, continue
   } catch (Exception e) {
       └─ Log exception, continue polling
   }
       │
       ▼
   Always: handler.postDelayed(...)
   (Polling continues regardless of errors)
```

## State Management

```
CustomerPresentation States:

IDLE
  │
  ├─► Cart Update Received → Store in currentCart
  │   (STATE: Ready to Print)
  │
  ├─► Polling for Print Jobs (Every 2 seconds)
  │
  └─► Print Job Found
      │
      └─► Connect to Printer
          │
          └─► Print Receipt
              │
              └─► Success: Log & Continue
              │
              └─► Error: Log & Continue
                  │
                  └─► Back to IDLE (Continue Polling)
```

This architecture ensures:
✅ Non-blocking print operations
✅ Continuous polling without freezing UI
✅ Proper resource cleanup
✅ Error resilience
✅ Professional receipt formatting

